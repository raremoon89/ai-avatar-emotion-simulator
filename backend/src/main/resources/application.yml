
server:
  port: 8080

spring:
  application:
    name: ai-avatar-backend

  datasource:
    url: jdbc:mysql://localhost:3306/emotion_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: pass1234
  jpa:
    hibernate:
      ddl-auto: update   # 개발 중엔 update, 운영 시엔 validate
    show-sql: true
    properties:
      hibernate.format_sql: true